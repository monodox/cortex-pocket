name: cortex_pocket
description: Cortex Pocket - On-device AI powered by optimized local LLMs
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  path_provider: ^2.1.1
  http: ^1.1.0
  shared_preferences: ^2.2.2
  flutter_markdown: ^0.6.18
  file_picker: ^8.0.0+1
  ffi: ^2.1.0
  crypto: ^3.0.3
  sqflite: ^2.3.0
  device_info_plus: ^9.1.0
  flutter_secure_storage: ^9.2.2
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/models/